export const API_URL = "http://localhost:8000";

export const VOITURE_URL = `${API_URL}/voitures`;

export const COMMENT_URL = `${API_URL}/commentaires`;

export const USER_URL = `${API_URL}/utilisateurs`;